std::string createConfig(std::string test2, std::string test3)
{
    int max(500); //The default max value is 500
    int configExit;
    std::string exitPrompt, configName;
    configName = "config";
    //Implement a checker
    for (configExit = 1; configExit < max; configExit++)
    {

    }
    exitPrompt = "Config file created";
    std::cout << test2 << "\n" << test3;
    return "Succesfully Executed";
}